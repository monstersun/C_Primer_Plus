#include <stdio.h>
#include <math.h>
#define SQUARES 64
#define CROPS 1E15
int main(void){
    long 
    printf("the king should pay: %ld\n", pow(2,SQUARES));
    printf("it almost %f times of the annual production in American.", pow(2, SQUARES)/CROPS);
}